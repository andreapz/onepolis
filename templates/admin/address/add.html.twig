{% extends 'admin/layout.html.twig' %}

{% block main %}

    {{ form_start(form) }}
    {# render the task's only field: description #}

    <h4>{{ 'label.address'|trans }}</h3>
    {{ form_row(form.street) }}
    {{ form_row(form.city) }}
    <ul class="list-group" id="cityresult"></ul>
    {{ form_row(form.postcode) }}
    {{ form_row(form.province) }}
    {{ form_row(form.state) }}
    
    <input type="submit" value="{{ 'label.create_address'|trans }}" class="btn btn-primary" />
    
    {{ form_end(form) }}

{% endblock %}




{% block javascripts %}
    {{ parent() }}

    <script>
       
       var queryurl =  "{{ path('city_handle_search') }}";
        
        
        
    </script>
{% endblock %}
